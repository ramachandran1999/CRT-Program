#include <stdio.h>
int main()
{
int a=100,b=25;
printf("a<<2=%d",a<<2);
printf("b>>2=%d",b>>2);
// left shift:
//shifting the bits on to left side
// and assigning 0's on to the right side
return 0;
}
