#include <stdio.h>
int main()
{
int a=10,b=10;
printf("a<<2=%d",a<<2);
printf("b>>2=%d",b>>2);
// left shift:
//shifting the bits on to left side
// and assigning 0's on to the right side
// right shift:
//shifting the bits on to right side
// and assigning 0's on to the left side
return 0;
}
